# Icon Assets

This directory stores branding assets for `vs-exclude`.

- `icon.png`: extension icon referenced by `package.json`
- `icon.svg`: editable source asset for future refinements

If you replace the icon later, keep a square image with enough contrast for both light and dark themes.
